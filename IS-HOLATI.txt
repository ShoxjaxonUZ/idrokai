═══════════════════════════════════════════════════════════════
  EduUz / Eduzy — IS HOLATI
  Sana: 2026-06-08
  Commit: b00f1ca (main, origin/main bilan sinxron)
═══════════════════════════════════════════════════════════════


BUGUN BAJARILGAN ISHLAR (2026-06-08)
─────────────────────────────────────────────────

Audit'dagi qolgan ishlar yopildi + dars sahifasi tuzatildi. 9 ta commit:

1. (8903396) DARS SAHIFASI — video resume race fix
   savedPositionRef fetch player onReady'dan keyin kelsa ham resume
   ishlaydi. ytPlayerRef/vimeoPlayerRef + seekToSaved() (avval
   aniqlanmagan edi — ReferenceError berardi).

2. (d256575) BACKEND TESTLAR — birinchi marta (audit kamchiligi)
   Node ichki runner (yangi dep yo'q): npm test.
   jsonParse, disposableDomains, geoip sof funksiyalari qoplandi.

3. (3990149) MODUL TEST KESHI — AI xarajati kamaydi
   module_test_bank jadval (migration 020): savollar (course_id,
   module_index) bo'yicha 1 marta generatsiya, prompt_hash bilan
   invalidatsiya. lib/quizShuffle.js personalize() har userda
   savol/variant tartibini aralashtiradi (anti-cheat).

4. (eeb2778) ABORTCONTROLLER — frontend useEffect fetch'larga
   Route param tez o'zgarganda stale javob + unmount setState
   muammosi yo'qoldi. 11 fayl (coursedetail, Lesson, CourseRatings,
   LessonQA, Leaderboard, Notifications, courses, AITeacher, profile,
   VerifyCertificate, NotificationBell).

5. (08d426f) EMAIL.JS — legacy Resend olib tashlandi (Brevo asosiy)
   Resend bepul rejada faqat o'z emailga yuborardi (o'lik kod).
   sendViaResend + RESEND env'lar tozalandi. Brevo + SMTP qoldi.
   Bonus: API kalit fragmenti log'ga chiqmaydi (maxfiylik).

6. (4f86337) AI QUIZ / MODULETEST DUBLIKATI birlashtirildi
   Backend Groq+parse+validatsiya → lib/quizGen.js (ai.js +
   moduleTest.js shuni ishlatadi). Frontend AIQuiz.jsx dagi o'lik
   modul-test kodi (localStorage block, course/test param) olib
   tashlandi — modul test = ModuleTest.jsx (server).

7-9. AUTH: JWT localStorage → httpOnly COOKIE + CSRF (2 bosqich)
   (9acd106) 1-bosqich dual-mode: lib/authCookies.js, middleware/
   csrf.js (double-submit), auth middleware cookie-first. cookie-parser,
   CORS credentials:true.
   (fa687f4) CSRF lockout fix: login/register/forgot/reset/resend
   CSRF'dan ozod (test paytida topilgan xato).
   (b00f1ca) 2-bosqich: localStorage'da REAL JWT yo'q — 'token' kaliti
   faqat sentinel ('cookie'). SSE cookie withCredentials.


HOZIRGI HOLAT
─────────────────────────────────────────────────

LOKAL:
  ✅ Backend ishlaydi (PostgreSQL ulanadi, migrations 020 gacha OK)
  ✅ Frontend build qiladi (Vite)
  ✅ Backend testlar: 30/30 o'tadi (npm test)
  ✅ Auth cookie/CSRF smoke testlar o'tdi (curl)

GIT:
  ✅ Commit b00f1ca origin/main'ga push qilingan

EMAIL: Brevo (BREVO_API_KEY) asosiy, SMTP fallback. Resend olib tashlandi.
AUTH:  Email tasdiqlash (Brevo). Login → httpOnly cookie. Telegram
       faqat contact + security alert uchun (ro'yxatdan o'tish emas).


AUTH COOKIE — DEPLOY TEKSHIRUVI (MUHIM!)
─────────────────────────────────────────────────

Prod'ga chiqqach quyidagilarni tekshiring:

1) RENDER env: CORS_ORIGIN aynan Vercel domeni bo'lsin (masalan
   https://eduzy.uz) — '*' EMAS. Credentials bilan wildcard ishlamaydi,
   aks holda cookie yuborilmaydi va HECH KIM kira olmaydi.
   NODE_ENV=production bo'lsin (cookie Secure+SameSite=None uchun).

2) Yangi login qiling → DevTools:
   - Application → Cookies: auth_token (HttpOnly ✓) + csrf_token
   - Application → Local Storage: 'token' qiymati = "cookie" (REAL JWT EMAS)

3) Sahifalarni aylanib chiqing (dars, test, profil, bildirishnoma).
   Bildirishnoma qo'ng'irog'i real-time ishlasin (SSE cookie orqali).

4) Eski sessiya: deploy'dan oldin kirgan userlar bir marta qayta login
   qilishlari kerak bo'lishi mumkin (cookie olish uchun). Bu normal.

5) Logout → cookie'lar tozalanishi kerak.


HALI QILINMAGAN (sizdan amal talab qiladi)
─────────────────────────────────────────────────

1) GROQ_API_KEY ROTATE — eski kalit audit'dan beri ochiq bo'lishi
   mumkin. Groq dashboard'da yangi kalit → Render env yangilash.

2) PRODUCTION PAROLLAR — DB password va ADMIN_PASSWORD kuchli
   qiymatlar bo'lishi shart (Render env'da). Default '1234'/'Admin1234'
   ishlatilmasin.

3) RESEND DOMEN emas — endi BREVO. Brevo dashboard'da sender email
   tasdiqlangan bo'lsin (Senders bo'limi), BREVO_FROM mos kelsin.


KELAJAK (texnik qarz, shoshilmaydi)
─────────────────────────────────────────────────
  - AI Quiz/ModuleTest backend birlashtirildi, lekin frontend 2 ta
    alohida sahifa (har xil UX — ataylab).
  - Teacher tizimi bor lekin kam ishlatiladi.
  - Route/DB darajasidagi backend testlar hali yo'q (faqat sof liblar).


FAYLLAR (siz tahrirlashingiz mumkin)
─────────────────────────────────────────────────
backend/.env              ← lokal sirlar (gitignored)
frontend/.env             ← VITE_API_URL (gitignored)
render.yaml               ← Render deploy config (CORS_ORIGIN izohi bor)
frontend/vercel.json      ← Vercel config


SAVOL TUG'ILSA, AYTING:
"hozir qayerdamiz" → bu fayl ochiladi va davom etamiz
"auth cookie ishlamayapti" → CORS_ORIGIN va NODE_ENV tekshiramiz
"GROQ kalitni rotate qilaman" → qadamlarni birga bajaramiz

═══════════════════════════════════════════════════════════════
